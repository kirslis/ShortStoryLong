import {styled} from "@mui/material";
import FlareIcon from "@mui/icons-material/Flare";

export const InspirationIconStyled = styled(FlareIcon)`
    color: ${(props) => props.theme.palette.textColor.main};
`